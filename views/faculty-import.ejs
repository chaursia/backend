<%- include('partials/head') %>
<div class="flex">
    <%- include('partials/sidebar') %>
    <main class="ml-64 flex-1 p-8 min-h-screen bg-gray-50">
        <div class="mb-8">
            <div class="flex items-center gap-2 text-sm text-gray-500 mb-1">
                <a href="/admin/faculty" class="hover:text-blue-600">Faculty</a>
                <span>/</span>
                <span class="text-gray-900 font-medium">Import JSON</span>
            </div>
            <h1 class="text-2xl font-bold text-gray-900">Import Faculty from JSON</h1>
        </div>

        <% if (locals.flash) { %>
        <div class="mb-5 flex items-center gap-2 <%= flash.type === 'success' ? 'bg-green-100 border-green-300 text-green-700' : 'bg-red-100 border-red-300 text-red-700' %> border text-sm px-4 py-3 rounded-xl">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
            <%= flash.message %>
        </div>
        <% } %>

        <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8 max-w-2xl mb-6">
            <h3 class="text-lg font-bold text-gray-900 mb-3">JSON Format</h3>
            <p class="text-sm text-gray-600 mb-3">Required fields: <code class="bg-gray-100 px-1.5 py-0.5 rounded text-xs font-mono">employee_id</code>, <code class="bg-gray-100 px-1.5 py-0.5 rounded text-xs font-mono">name</code>. Existing records (matched by employee_id) will be updated.</p>
            <pre class="bg-gray-900 text-gray-100 text-xs rounded-xl p-4 overflow-x-auto"><code>[
  {
    "employee_id": "FAC001",
    "name": "Dr. John Smith",
    "designation": "Professor",
    "department": "Computer Science",
    "email": "john@college.edu",
    "phone": "9876543210",
    "qualification": "PhD",
    "specialization": "Machine Learning",
    "profile_image": "https://example.com/photo.jpg",
    "is_active": true
  }
]</code></pre>
        </div>

        <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8 max-w-2xl">
            <form method="POST" action="/admin/faculty/import">
                <div>
                    <label class="text-sm font-semibold text-gray-700 block mb-1.5">JSON Data <span class="text-red-500">*</span></label>
                    <textarea name="jsonData" rows="15" class="w-full text-sm font-mono border border-gray-200 rounded-xl px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-blue-500/30 focus:border-blue-400" required placeholder='[
  {
    "employee_id": "FAC001",
    "name": "Dr. John Smith",
    "designation": "Professor",
    "department": "Computer Science",
    ...
  }
]'></textarea>
                </div>
                <button type="submit" class="mt-5 bg-blue-600 hover:bg-blue-700 text-white text-sm font-semibold px-6 py-2.5 rounded-xl transition-colors shadow-sm">Import Faculty</button>
            </form>
        </div>
    </main>
</div>

</body>
</html>